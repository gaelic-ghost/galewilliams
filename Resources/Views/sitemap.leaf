#extend("layout"):
    #export("body"):
        #extend("components/page-intro")

        <section class="sitemap-list" aria-label="Public site pages">
            #for(entry in entries):
                <a class="sitemap-entry" href="#(entry.path)">
                    <h2>#(entry.label)</h2>
                    <p>#(entry.summary)</p>
                </a>
            #endfor
        </section>
    #endexport
#endextend
